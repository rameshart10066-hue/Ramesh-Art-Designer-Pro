# components/

Shared, reusable, presentation-focused UI components (buttons, inputs, cards,
layout primitives). No module-specific business logic here — if a component
only makes sense inside one feature module, it belongs in that module's own
folder under `src/modules/<module>/components/` instead.
