# lib/

Server-side helpers used by API routes (src/app/api/**): validation,
request/response mapping, orchestration between packages/* engines and
the database package. Keep framework glue here — not business logic,
which belongs in packages/*.
