/**
 * @ramesh/ai-engine
 *
 * Single point of contact for all Claude API calls across the platform:
 * AI design assistance, ad copy / marketing content generation, and any
 * future AI-powered module. Centralizing this here means API key handling,
 * retry/error logic, and prompt templates live in one auditable place
 * instead of being duplicated per feature module.
 *
 * No feature code yet — scaffold-only.
 */

export const AI_ENGINE_VERSION = "0.1.0";
