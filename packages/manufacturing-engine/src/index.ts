/**
 * @ramesh/manufacturing-engine
 *
 * Translates design-engine output into machine-ready instructions:
 * LightBurn-compatible SVG/export formatting, kerf compensation, material
 * and machine profiles, cut-time estimation. Depends on @ramesh/design-engine
 * for shape data but knows nothing about the UI layer.
 *
 * No feature code yet — scaffold-only.
 */

export const MANUFACTURING_ENGINE_VERSION = "0.1.0";
