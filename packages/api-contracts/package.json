{
  "name": "@ramesh/api-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and request/response schemas between the frontend and API routes. Single source of truth to prevent frontend/backend drift.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
